<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">はぐくみ</a>
  <div class="collapse navbar-collapse" id="Navber">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          MENU
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to '会社' , {controller: :companies, action: :index} , id: :link, class: 'dropdown-item' %>
          <%= link_to '全社員' , {controller: :users, action: :index} , id: :link, class: 'dropdown-item' %>
          <%= link_to '全社員詳細' , {controller: :user_years, action: :index} , id: :link, class: 'dropdown-item' %>
        </div>
      </li>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>


<p id="notice"><%= notice %></p>

<h1>会社</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>健康保険社員負担率</th>
      <th>健康保険介護保険率</th>
      <th>厚生年金保険率</th>
      <th>厚生年金基金社員負担率</th>
      <th>雇用保険率</th>
      <th>金利(想定金利)</th>
      <th>月の所定労働時間</th>
      <th>適用最低賃金</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.kenpo_charge_employee_rate %></td>
        <td><%= company.kenpo_kaigo_rate %></td>
        <td><%= company.kosei_rate %></td>
        <td><%= company.kikin_charge_employee_rate %></td>
        <td><%= company.koyou_rate %></td>
        <td><%= company.interest_rate %></td>
        <td><%= company.monthly_base_work_hour %></td>
        <td><%= company.min_instalment %></td>
        <td><%= link_to '社員一覧', controller: "users", action: "index", companies_id: company.id %></td>
        <td><%= link_to 'Show', company %></td>
        <td><%= link_to 'Edit', edit_company_path(company) %></td>
        <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company', new_company_path %>
